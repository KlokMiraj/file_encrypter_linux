#include <stdio.h>
#include <stdlib.h>

int main(){
	int cout;
	char name[cout];
	
	
		//name=(char*)calloc(cout,sizeof(char));
		 cout=sizeof(name)/sizeof(char);
			
cout++;
	for(int i=0; i<cout;i++){
	scanf("%c",&name[i]);
	}
	printf("The array have yeti elements: %d",cout);
	
	//free(name);
	
	//printf(" %c",name);

return EXIT_SUCCESS;
	}

	
